import React from 'react'

const Pagination = () => {

  return (
    /*
    <div className="pagination">
      <p>
        Showing {start}-{perPage * start} of {routes.length} routes.
    </p>
      <p>
        <button key="previous" disabled={start === 1} onClick={prevPage}>
          Previous Page
        </button>
        <button key="next" disabled={start + perPage >= routes.length} onClick={nextPage}>
          Next Page
        </button>
      </p>
    </div>
    */
    <h2>Pagination</h2>
  )
}

export default Pagination
